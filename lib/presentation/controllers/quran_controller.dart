import 'package:get/get.dart';

class QuranController extends GetxController {
  // We can add search logic and other state management here later.
  final count = 0.obs;
  @override
  void onInit() {
    super.onInit();
  }

  @override
  void onReady() {
    super.onReady();
  }

  @override
  void onClose() {
    super.onClose();
  }

  void increment() => count.value++;
}
